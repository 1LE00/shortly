import Button from '../button/Button';
import './boost.css';
const Boost = () => {
    return (
        <section className='boost flex flex-column'>
            <h2>Boost your links today</h2>
            <Button name='Get started' />
        </section>
    )
}

export default Boost